import ReactDOM from 'react-dom/client';

//importing a css in a js file
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
